<%= render partial: '/jobs/display_errors', locals: {job: @customer} %>

<%= form_for @customer do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name, autofocus: true %><br>
  <%= f.submit %>
<% end %>
